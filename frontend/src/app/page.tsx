"use client";

import UploadForm from "../components/uploadForms";

export default function HomePage() {
  return (
    <main className="min-h-screen flex justify-center  bg-gray-50 text-gray-900">
        <UploadForm />
    </main>
  );
}
